services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: invoice_db
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
